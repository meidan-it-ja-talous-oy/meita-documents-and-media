{
	"name": "bucket-browser-block",
	"version": "1.0.0",
	"description": "Meita gutenberg bucket-browser block",
	"author": "Meidän IT ja Talous Oy",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "rm -rf dist && wp-scripts build && mkdir dist && cp -r build dist && cp bucket-browser-block.php dist/ && cp block.json dist/ && bash ../../deploy-local.sh",
		"format:js": "wp-scripts format-js",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"start": "rm -rf dist && mkdir dist && cp block.php dist/ && cp block.json dist/ && wp-scripts start --output-path=dist/build",
		"packages-update": "wp-scripts packages-update"
	},
	"devDependencies": {
		"@wordpress/scripts": "^12.2.0"
	}
}
